# Changelog

## 2026-07-31 — Fix: Sessions page failed layout on open
- The account shell (`my_account/root.dart`) wrapped every sub-page in a `SingleChildScrollView`. That suits Profile and Security, which are plain `Column`s, but the Sessions page brings its own `ListView` for pull-to-refresh — a scrollable inside a scrollable, so the inner viewport got unbounded height and threw on layout the moment the page opened. Both the loaded list and the shimmer skeleton hit it.
- `_AccountSubPage` gained a `scrollable` flag (default `true`); Sessions is pushed with `scrollable: false` and now owns the gutter padding itself, so its `RefreshIndicator` works as intended.
- Added `test/modules/main/more/my_account/sessions_page_test.dart` — opens Sessions from the hub and asserts no layout exception, loaded and loading. Both cases reproduce the crash when the shell fix is reverted.
- Modules affected: `modules/main/more/my_account/` (`root.dart`, `sessions_page.dart`)

## 2026-07-31 — Sessions page is real
- **`api/session_api.dart`** — `getSessions()`, `revokeSession(id)`, `revokeOtherSessions()` against the self-scoped `/users/me/sessions` routes (no client_id: a session belongs to the person, not a workspace membership)
- **`repository/models/session_model.dart`** — `SessionModel`, with almost every field nullable because the backend returns one only when it genuinely knows it. `displayName`/`displayContext` join what's present and omit what isn't, rather than filling gaps with placeholders
- **`repository/providers/sessions_provider.dart`** + **`notifiers/auth/revoke_session_notifier.dart`** — query and mutation. Deliberately not keepAlive: the list goes stale the moment another device signs in or out
- **`my_account/sessions_page.dart`** rewritten off the fixture — shimmer skeleton on first load only (`hasValue`/`isLoading`, not `.when()`), pull-to-refresh, and `ref.invalidate` after each revoke
- The hub's "N signed-in devices" subtitle now watches the same provider, so it can't disagree with the page it opens
- A client-reported location carries a "Reported by device" pill — `location_source: CLIENT` means spoofable, and this screen exists for spotting access that isn't yours
- Revoke-all reports the count the **server** returns, not the local list length; the two differ if a session expired in between
- Affected modules: `api/`, `repository/models|providers|notifiers/`, `modules/main/more/my_account/`

## 2026-07-31 — Session metadata carries locale and a reported place
- **`collectSessionMetadata` now sends `locale`** — `language` from `PlatformDispatcher`, and `timezone` (IANA, e.g. `Africa/Accra`) from the new **`flutter_timezone`** dependency. The backend derives a representative city from the zone, so a session shows a place without a permission prompt, a paid IP lookup, or any third-party call
- **`app.name` is now sent.** A native client has no browser to name it, so without this the sessions list showed no client at all — only a version
- Location is client-reported and the backend stamps it `location_source: CLIENT`, so the UI must present it as reported by the device, never as verified
- The locale block is wrapped like the others: `flutter_timezone` has no test-host implementation and always throws there, which must degrade to "no locale" rather than failing a login. Covered by a regression test
- Affected modules: `lib/src/lib/session_metadata.dart`, `pubspec.yaml`

## 2026-07-30 — Home avatar opens My Account
- **The manager avatar in Home's `_TopHeader` was inert** — a bare `RLAvatar` with no tap handling, even though it reads as the standard "go to your profile" affordance. Wrapped in a `GestureDetector` that pushes `/more/my-profile`, matching the notification button directly beside it (haptic → `context.mounted` guard → `context.push`)
- Tapping it lands on `MyAccountScreen`, the same destination as More's profile card — one profile entry point, two ways in
- **The `GestureDetector` needs `behavior: HitTestBehavior.opaque`** — `RLAvatar` paints itself with a `BoxDecoration`, and `RenderDecoratedBox.hitTestSelf` is false, so the default `deferToChild` made only the centred initials tappable and the avatar read as broken. This is the same reason `RLRow` sets it. Worth remembering for any future `GestureDetector` wrapped around a decoration-only widget — `RLIconBtn` has the same latent shape (its live area is just the icon glyph, not the full 38×38 box)
- Affected modules: `modules/main/home/`

## 2026-07-30 — My Account redesign (UI only)
- **`more/my_profile.dart` deleted, replaced by the `more/my_account/` module** — the old screen was a flat, fully-hardcoded mock (name and email were string literals). The redesign mirrors the web portal's Settings › My Account: a hub listing three categories, each pushing its own page rather than one long scroll — Profile (photo, name, email) · Security (password, 2FA, delete account) · Sessions (signed-in devices, revoke one or all others)
- **The route path `/more/my-profile` was kept**, now building `MyAccountScreen`, so `more/root.dart`'s profile card needed no change
- **Real data is limited to identity and sign-out** — name/email/role come from `currentUserNotifierProvider`, and "Sign out of this device" calls the real `appStartupNotifierProvider.logout()`. Everything else is UI against `placeholder_data.dart`: the 4-device session list is a fixture (revoking filters local state, nothing is sent), and the email-verified badge / password-last-changed date are constants
- **Two-factor, email updates and account deletion are deliberately unbuilt** — rather than shipping dialogs that can't submit, each control stays visible with a "Coming soon" pill and fires `showAccountComingSoon(ref, feature)`. This matches the web portal, where the same three were parked in the same pass
- **New shared pieces**: `sheets.dart`'s `_AccountSheet` chrome follows `properties/edit_sheets.dart`'s `_PSSheet` convention (drag handle, title/desc, scrolling body, footer); `_PasswordMeter` ports the web `PwMeter` scoring verbatim; `widgets.dart`'s `AccountField` is the label-above/value+action-below row used by Profile and Security
- Affected modules: `modules/main/more/` (new `my_account/`, `my_profile.dart` removed), `navigation/routes.dart` (import + builder)

## 2026-07-30 — Money always shows two decimals; login email normalised
- **`formatCedis(num)` added to `lib/money.dart`** — thousands separators and always two decimals. Converting pesewas → cedis is arithmetic (`pesewasToCedis`); deciding how the result *looks* now has exactly one home instead of three divergent ones
- **`RLMoney` was rounding to whole cedis** (`v.round()`), so a rent of 600050 pesewas displayed as `6,001` — both decimal-less and *wrong by half a cedi*. It now delegates to `formatCedis`. This is the visible change: rent figures on the lease hero card, start-lease sheet, unit detail and property detail (plus the seed-backed home/booking/invoice totals) go from `6,000` to `6,000.00`
- **`leases/detail.dart`'s `_moneyLabel` had decimals but no grouping** (`6000.00`); it now calls `formatPesewas`, so the lease Financial Terms grid matches everywhere else
- **`formatPesewas` moved out of `activity/application_data.dart` into `lib/money.dart`** — a money formatter living in one screen's seed-data file meant the application screens and the rest of the app could drift. Its named arg changed from `currency` (a symbol) to `code` (an ISO code) so it resolves the sign through `currencySymbol`; all 11 call sites passed positionally, so none needed touching beyond the import. Its three test cases moved to `test/lib/money_test.dart`
- **`edit_unit.dart` prefilled the rent field as `60.0`**, not `60.00` — now `toStringAsFixed(2)`, matching the equivalent prefill in `application_edit_sheets.dart`
- **Login email is normalised before submit** — new `lib/email.dart` `normalizeEmail()` (trim + lowercase), applied in `LoginNotifier.submit` rather than at the text field, so the screen still shows what was typed and no caller can submit a cased address. iOS autocapitalises the first letter of an email field, which otherwise fails the lookup against an address stored lowercase

## 2026-07-30 — Currency codes render as signs, not ISO codes
- **`currencySymbol(String code)` added to `lib/money.dart`**, mirroring `apps/go`'s `MoneyLib.currencySymbol`: `GHS`→`GH₵`, `USD`→`$`, anything else passes through unchanged so a currency the backend adds later still renders something readable rather than blank
- **Every user-facing `GHS` is now a sign.** Two screens had each open-coded the same ternary (`activity/maintenance_detail_tabs.dart`'s `_money`, `leases/detail.dart`'s `_moneyLabel`) and four rendered the raw literal — `properties/add_unit.dart` and `properties/edit_unit.dart`'s Currency field, `activity/add_booking.dart`'s nightly-rate label and live-summary line. `properties/unit_detail.dart`'s Currency row was printing `unit.rentFeeCurrency` straight through
- **Write paths keep the ISO code**, now via the new `defaultCurrencyCode` const rather than a scattered `'GHS'` literal — `create_property_notifier.dart`, `add_unit.dart`, `edit_unit.dart`, `activity/application_data.dart`. The symbol is display-only; nothing sends `GH₵` to the API
- **Two money listings were rendering a bare number with no currency at all** — `more/billing.dart`'s invoice-card amount (`'${invoice.amount}'`) and `money/root.dart`'s invoice-row amount (`_fmt(inv.amount)`). Both now carry a symbol
- **Eight more used a bare `₵` instead of `GH₵`**, which read as a different currency next to the `GH₵` totals on the same screen: `money/root.dart` (paid-so-far, expense row), `money/invoice_detail.dart` (line item, total, amount received), `home/root.dart` (overdue-invoice tile), `tenants/detail.dart` (rent, balance — including the `₵0` zero case). All normalised through `currencySymbol`
- These four screens are still seed-backed (no API wiring yet); the fix is to the display path they'll keep using

## 2026-07-29 — All cross-property lists share one access rule
- **`/leases`, `/tenants`, `/invoices`, `/maintenance-requests`, `/expenses`, `/units`, `/insights/risk-properties` and `/tenant-applications` now scope identically**: every row is bounded to the caller's `ClientUserProperty` links, enforced inside the query, for every role — no OWNER-unrestricted path anywhere. Previously two different idioms coexisted and both let an OWNER see properties they held no link to, including ones absent from their own filter chips
- New shared `accessiblePropertyIDsSubQuery` (`repository/common.go`); each repository has a small scope over it, since tables reach a property differently — direct `property_id` (units, expenses, invoices, tenant applications), via `unit_id` (leases, maintenance requests), or via the lease/booking join in `propertyMatchCondition` (tenants)
- `ValidateRequestedPropertyAccess` replaces `ResolvePropertyScopeFilter` at these call sites: no query at all unless a `property_id` was requested, then one bounded lookup, purely so an unreachable property answers 403 rather than an empty list
- `InjectPropertyAccessScopeMiddleware` dropped from the route group — with the boundary in the SQL it was running a `client_user_properties` query per request that nothing read. `/announcements` keeps it and its previous behaviour, deliberately: it is used by the web portal and was out of scope here
- **Mobile-only behaviour change**, confirmed: the web portal calls the property-scoped routes, not these

## 2026-07-29 — Applications list: real data, filters + shared checklist lib
- **New cross-property endpoint.** `GET /api/v1/admin/clients/{client_id}/tenant-applications` (`TenantApplicationHandler.ListTenantApplicationsAcrossProperties`). The existing `ListTenantApplications` is mounted only under `/properties/{property_id}/`, and the Activity tab is not property-scoped, so the mobile list had nothing to call. `ListTenantApplicationsQuery` gained `ClientUserID` / `PropertyIDs` / `DesiredUnitIDs` with three new scopes registered on **both** `List` and `Count`
- **Scoping is by `ClientUserProperty` for every role, and it lives in the SQL.** `tenantApplicationClientUserAccessScope` semi-joins `client_user_properties` inside both `List` and `Count`, so results can only come from properties the caller is linked to — no role branching, and it cannot be widened by a handler forgetting a filter. The route deliberately does *not* use `ResolvePropertyScopeFilter` (which short-circuits OWNER to unrestricted, client-wide access, as the five siblings do). `ValidateRequestedPropertyAccess` only checks an explicitly supplied `property_id`, so there is **no extra query at all** in the common case and only a lookup bounded by the number of ids requested otherwise — deliberately not resolving the caller's full property list, which would grow the statement with their portfolio and hit Postgres's parameter ceiling. Registered outside the `InjectPropertyAccessScopeMiddleware` group, which would otherwise run its own link query per request without being consulted
- **`activity/applications_list.dart` replaces the `_kApps` fixture** in `activity/root.dart`. Debounced (350ms) search bar over `query`+`search_fields=first_name,last_name,email,phone`, then five filter chips — Property, Unit (both multi-select), Status, Gender, Marital Status — reusing the maintenance board's own chip and sheet widgets. Infinite scroll, shimmer skeleton, pull-to-refresh, and empty states that distinguish "none yet" from "none matching"
- **The card's progress bar shows only while an application is pending** (`Status.InProgress`) — a completed one is complete by definition, a cancelled one moot
- **New `lib/application_checklist.dart` is now the single source of truth** for what "complete" means, ported one-for-one from the web's `use-calculate-checklist.ts` and its five `checklist-*.ts` builders. The seed-backed detail screen delegates to it via `ApplicationDetailData.toApplicationModel()`, so the live list and the seeded screen cannot disagree. The seed's own docs section had been a simplification — it lacked the web's ONLINE/MANUAL mode handling and its witness rows parsed out of the document's Lexical content (`lib/document_utils.dart`, also new)
- **`TenantApplicationModel` expanded** from the handful of financial fields the lease detail screen reads to the full listing shape, plus `ApplicationDocumentModel` / `ApplicationDocumentSignatureModel` and `InvoiceRef.status`. All additions nullable; both call sites are fed by the same `DBAdminTenantApplicationToRest`
- **Tapping a card no longer opens a fixture.** The list passes the fetched row as GoRouter `extra` and `ApplicationDetailData.fromApplicationModel()` renders it. The detail screen is otherwise still seed-backed — integrating it is a follow-up
- Renamed `providers/activity/maintenance_filter_options_provider.dart` → `activity_filter_options_provider.dart` (`activityFilterProperties`/`activityFilterUnits`); extracted `RLFilterTriggerChip`/`RLFilterSheet`/`RLFilterPickResult`/`rlMultiSelectChipLabel` from `maintenance_board.dart` into `activity/activity_filters.dart` so both tabs share one implementation
- Applications tab count in the Activity header is now the real `meta.total`; Maintenance and Bookings stay hardcoded

## 2026-07-27 — Application Info: hub + section pages redesign (UI only)
- Rebuilt `activity/application_detail.dart` from the Claude Design source (`Application Info (mobile).html` → `ScreenApplicationDetail` in `rl-mgr-application.jsx`). The old screen was one long scroll (applicant card → onboarding steps → lease preview → info banner); it is now a **hub + per-section pages**: the hub carries the hero (`#code` · status pill · "Application Info" · submitted line) and the "Complete application info" checklist, and each checklist row pushes its own page
- **Scope is deliberately UI-only** — pm_mobile still has no tenant-application API layer, so the screen remains seed-backed. The seed model (`activity/application_data.dart`) is shaped field-for-field like the REST payload (`first_name`, `stay_duration_frequency`, `rent_fee` in **integer pesewas**, `employer_type`, …) so wiring it to a real `TenantApplicationModel` is a swap rather than a rewrite. The backend and web portal already expose the endpoints (`app/api/tenant-applications/`)
- New `activity/application_detail_pages.dart` — five section pages mirroring the web's own route split (`.../applications/$applicationId/{,tenant-details,move-in,financial,docs}`): **Unit Setup**, **Tenant Details** (Basic Information + Identity Verification + Emergency Contact & Background on one page, as both the design and the web do), **Move In Setup**, **Financial Setup** (rent/deposit summary + Initial Payment Setup + live invoice summary), **Docs Setup** (document row, editor hint, per-signer signing status). Shared building blocks — `AppSectionCard`, `KVGrid`/`KVEntry` (2-col key/value grid with full-width spans), `CardSubheading`, `MonoLabel`, `EditButton` — live here rather than in `shared/widgets.dart`, since nothing outside this feature uses them yet
- New `activity/application_edit_sheets.dart` — every "Edit" affordance opens a bottom sheet carrying the **real editable fields, ported from the web forms** (`apps/property-manager/.../application/{tenant/basic,tenant/identity,tenant/emergency-contact,move-in,financial/*}`), including the same enum option sets (`MALE|FEMALE`, `SINGLE|MARRIED|DIVORCED|WIDOWED`, `NATIONAL_ID|PASSPORT|DRIVER_LICENSE|GHANA_CARD`, `STUDENT|WORKER`, `HOURLY|DAILY|WEEKLY|MONTHLY`, `ONE_TIME_PAYMENT|CUSTOM`) and the same required-field rules, so validation won't drift when the API lands. Sheets pop with an updated immutable sub-model (or null on cancel); Save is disabled while invalid. The employment form switches labels/fields on `employer_type` exactly like the web (Institution/School vs Employer, occupation pinned to `STUDENT` for students). Also holds the design's Add-document sheet, the unit picker, and the post-approval What's-next sheet
- **Unit Setup page + picker** — ported from the web's `application/unit/index.tsx` and `change-unit-modal.tsx`, which the mobile design had no board for (it aliased "Select a unit" to Tenant Details). Shows the assigned unit as a card (cover image / "No image", status pill, name, type, market rent) with View unit + Change, or an empty state with Assign unit. The picker lists every unit with status and rent, marks the current one, and **only lets an Available unit be chosen** (the currently-assigned one stays selectable even if it isn't) — matching the web exactly. Selecting a unit **cascades**: `ApplicationDetailData.withUnit` copies the unit's `rent_fee` and `payment_frequency` into the financial setup and its frequency into `stay_duration_frequency`, mirroring the fields the web PATCHes alongside `desired_unit_id`; the UI says so in a snackbar rather than silently rewriting the rent. Changes lock once the initial payment is paid or anyone has signed the lease doc (with the reason shown inline), and are hidden entirely on a single-unit property or an approved application
- **Progress is derived, never stored.** `ApplicationDetailData` computes the five checklist sections exactly as the web's `use-calculate-checklist` does — 17 tenant fields, 3 move-in, 4 financial, `1 + one-per-signer` docs — with progress as the fraction of *fully complete* sections and an unattached document yielding an empty section that passes the approval gate vacuously while still counting as incomplete for display. Editing any section therefore moves the bar for real. Note the design mock hardcodes `40%` against data that derives to `20%`; the derived value is used
- Interactions are live rather than decorative: checklist rows navigate, edits round-trip into the hub's state, Approve is **gated on `canApprove`** (naming the outstanding sections in a snackbar instead of approving), approving confirms then flips to the "Lease is created" banner + unit card + "What's next?" action bar, and the manager's own signer row signs locally while the other three send a prompt. Document upload/picker remains a toast — there is no upload path on mobile yet
- Uses only existing `RL*` primitives and `RLTokens`; the design's `MG` token set is already a 1:1 match for `RLTokens`. Approved status maps to `RLTone.success` explicitly rather than being added to `statusTone()`, per the standing note that `Completed` stays on the neutral default there
- **Five layout bugs found and fixed by driving the screens at 402pt and 375pt.** In severity order: (1) the Add-document sheet's template grid used `crossAxisAlignment: CrossAxisAlignment.stretch` inside the sheet's unbounded-height scroll view, which asks children for *infinite* height and **hard-crashes layout** — wrapped in `IntrinsicHeight`, which bounds the row to its tallest child while still equalising the two cards; (2) the unit card's "Market rent" row used `Spacer()` between unconstrained `Text`s and overflowed ~126px once the amount grew — rebuilt as label + one `Expanded` holding a right-aligned `Text.rich` that ellipsizes; (3) `_FieldLabel` overflowed on long labels like "Number of months (max 12)" beside the "Optional" badge — label is now `Flexible` so it wraps; (4) the invoice **Total** row (in both the Financial page and the sheet's preview) overflowed on large amounts — value now right-aligns inside an `Expanded` and ellipsizes; (5) the Add-document filter chips were a `Row` that overran a 375pt screen — now a `Wrap`. Items 2 and 4 are the same `Spacer`/unconstrained-`Text` trap already documented for `_SpecRow` on the MR detail screen; it keeps recurring because it looks fine at the width you happen to build at
- **Shared primitive fix — `RLSegmented` labels overflowed instead of ellipsizing.** The segment label `Text` sat unconstrained inside the button's centred `Row`, so any segment whose label outgrew its equal-width slot overflowed rather than truncating (surfaced by "Manual Upload / From Library" at 375pt, i.e. an iPhone SE). Wrapped in `Flexible` with `maxLines: 1` + ellipsis. Same class as the `RLBtn` label fix logged on 2026-07-26, and likewise fixes every segmented control in the app
- **These were only found because the screens were actually driven.** `flutter analyze` reports none of them — overflow and infinite-constraint bugs surface at layout time, not compile time. Per project preference the repo keeps no widget tests for this feature, so the checks were run from a temporary throwaway test file that was deleted afterwards; anything similar in future needs the same manual pass at both 402pt and 375pt
- New `test/modules/main/activity/application_data_test.dart` — pure-logic coverage only (checklist derivation, unit-selection cascade and lock rules, financial totals incl. deposit handling, pesewa formatting, period pluralisation). No widget tests, per project preference
- Modules affected: `modules/main/activity/` (`application_detail.dart` rewritten; `application_data.dart`, `application_detail_pages.dart`, `application_edit_sheets.dart` new). No routes, models, providers or backend changes

## 2026-07-27 — Fix cedi sign (`₵`) rendering as tofu boxes on Android
- Root cause: none of the three bundled families (`DMSans`, `DMSerifDisplay`, `JetBrainsMono`) has a glyph for U+20B5, verified by reading each TTF's `cmap` table; Android's system fallback chain doesn't reliably supply it either, so every `GH₵`/`₵` string rendered a box
- Bundled `assets/fonts/InterCurrency[opsz,wght].ttf` — a 24 KB subset of Inter (U+0024, U+00A2–U+00A5, U+20A0–U+20BF) that retains the `wght` axis so the glyph matches surrounding text weight — and declared it as family `InterCurrency` at weights 100–900
- Added `RLTokens.fontFallback` and wired it through `buildTheme()`: `ThemeData(fontFamilyFallback:)`, `textTheme.apply(fontFamilyFallback:)`, plus the three inline styles that bypass the text theme (app-bar title, elevated-button label, input hint). The ~800 widget-level `TextStyle`s that set only `fontFamily` inherit the fallback via `DefaultTextStyle` merge — no per-widget changes needed
- Added `test/shared/font_coverage_test.dart`: parses every bundled TTF's `cmap` and fails if a currency-block rune used anywhere in `lib/` has no glyph in any bundled font; also asserts the theme's styles carry the fallback. Verified it fails without the fix
- Modules affected: `assets/fonts/`, `pubspec.yaml`, `shared/tokens.dart`, `shared/theme.dart`, `test/shared/`

## 2026-07-26 — Maintenance request detail: redesign + read path
- Rebuilt `activity/maintenance_detail.dart` against the Claude Design source (`Maintenance Request (mobile).html` → `ScreenMRDetail`): hero card (code · status pill · serif title · opened · description), attachments strip, Assignments card (Worker · Manager), Properties card (Priority · Category · Visibility · Unit · Lease), History/Comments/Expenses segmented tabs, created/updated footer, sticky Assign · Change status bar. Built entirely from existing `RL*` primitives — no new shared widgets
- **The screen was 100% hardcoded mock data (`_kMaint`) before this** — it never called the API, so tapping a real request from the (already-wired) board showed unrelated seed data. All read data is now real
- New API methods on `MaintenanceRequestApi`: `getMaintenanceRequest` (single fetch, populate mirrors the web portal's `server.ts`), `getActivityLogs`, `getComments`, `getExpenses` — the three sub-resources share one `_getRows` helper since all are `{data:{rows,meta}}` under the same request path. `getMaintenanceRequests`' `statusLabel` became optional (the backend reads `status` as a repeatable param and skips the filter when absent)
- New models: `MaintenanceActivityLogModel`, `MaintenanceCommentModel`, `MaintenanceExpenseModel` — all reuse the existing `MaintenanceAssigneeModel` for actor fields, since `performed_by_client_user`/`created_by_client_user` return the same `{id, user:{name}}` shape. `MaintenanceRequestModel` gained `attachments` (defaults `[]`), `visibility` (defaults `TENANT_VISIBLE`), `leaseId`, `reviewedAt`
- New `repository/providers/activity/maintenance_detail_provider.dart` — four family providers keyed on `(requestId, propertyIdHint)`. The three tab providers are lazy, so opening the screen costs one call, not four
- **Property-id threading**: every detail endpoint is property-scoped but the route carries only the request id. The board now passes `unit.property_id` via GoRouter `extra`; `maintenanceRequestPropertyId` recovers it from the cross-property list when absent (deep link / cold start), so the route stays a plain `/:id`
- Extracted the board's cancel-reason sheet into `activity/maintenance_status_actions.dart` (`MaintenanceCancelReasonSheet`, `confirmMaintenanceResolve`, `promptMaintenanceCancelReason`, `pickMaintenanceStatus`) — the board and the detail screen must gate Resolved/Cancelled identically, so those prompts now live in one place instead of drifting
- **Spec rows**: the five editable values (Worker, Manager, Priority, Category, Visibility) render as plain right-aligned text with a trailing chevron — the chevron is what marks a row editable. Only Priority renders as a tag (`RLPill`, tone-coded): it is the one value carrying a status meaning, so it earns the colour, where wrapping a person's name or a category in a tag would imply a status it doesn't have. `Unassigned` dims to `mutedSoft` since it's a prompt to act rather than a value. Unit and Lease deliberately differ in both colour and glyph — crimson text plus a forward arrow: a chevron edits something on this screen, an arrow leaves it
- **Trailing-glyph alignment**: `_SpecRow` gives all leftover width to a single `Expanded` and right-aligns the trailing group inside it. An earlier revision used `Spacer()` plus `Flexible(trailing)` — both default to `flex: 1`, so Flutter split the leftover space 50/50 and laid the trailing group out at the *left* edge of its half, leaving short values like `Medium` floating mid-row while long ones like `Gideon Bempong` looked nearly correct. Covered by a regression test (below)
- **Unit and Lease navigation is live** — both destinations already existed (`/properties/:id/units/:unitId` and `/more/leases/:id?property_id=`), and the request carries `unit.property_id`, `unit_id` and `lease_id`, so both rows now push for real. Each is rendered only when its property scope is resolvable, so a row is never shown as a link it can't follow
- Status change is live on the detail screen (reuses `MaintenanceRequestStatusNotifier`); Assign, post comment, add expense and the five tokenized field-edits surface a "Coming soon" toast rather than dead-tapping — the rows are already built as tap targets, so wiring each one is a matter of swapping the placeholder for its sheet
- Attachments render as images with a labelled file fallback (bare URLs, no MIME hint) and open externally via `url_launcher`; no `cached_network_image` dependency was added
- Modules affected: `api/maintenance_request_api.dart`, `lib/maintenance_utils.dart` (added visibility mapping), `repository/models/` (3 new + 1 extended), `repository/providers/activity/` (new), `modules/main/activity/` (`maintenance_detail.dart` rewritten, `maintenance_detail_tabs.dart` + `maintenance_status_actions.dart` new, `maintenance_board.dart` rewired), `navigation/routes.dart`
- **Backend Swagger fix (`services/main`)**: `transformations.OutputMaintenanceActivityLog` declared no `performed_by_client_user` field even though `DBMaintenanceActivityLogToRest` has always emitted one — the generated Swagger spec under-reported the activity-log response, so anyone building against the docs (including this screen's History tab) would not have known the performer was available. Added it as `*OutputClientUser` (nullable — `DBClientUserToRest` returns nil for an absent user) and regenerated docs. Also corrected `OutputMaintenanceRequestComment.CreatedByClientUser` from a value `OutputClientUser` with a no-op `omitempty` to `*OutputClientUser`, matching the same transform's nullable output; this changes no Swagger output (swaggo renders struct and pointer-to-struct identically) but makes the Go type honest
- New `test/modules/main/activity/maintenance_detail_test.dart` — the first pm_mobile widget test with real provider overrides (the existing board test only mounts a bare widget). Its fixture deliberately mixes a wide value against a narrow one, since equal-width values hide trailing-glyph misalignment entirely, and it asserts all seven trailing glyphs (5 chevrons + 2 arrows) share one right edge within 0.5px
- **History timeline narrates like the web portal.** Ported `ActivityTab`/`ActivityDetail` from `apps/property-manager/.../request/activity-tab.tsx` so both clients tell the same story. The backend never writes `description` on an activity log, so every sentence is composed client-side from `action` + `metadata` + the request, rendered via `Text.rich` with per-segment emphasis (values and names in ink-semibold, "(assigned to themselves)" italic) and an inline avatar for the person named. Branches: CREATED distinguishes manager-filed / filed-on-behalf-of-tenant / tenant-submitted; STATUS_CHANGED reads `metadata.from`/`to` and maps the raw enums through `mrStatusLabel`; assignments read the assignee **off the request**, not the log
- **Assignment entries inherit a known web-portal imprecision**: the log records only who *performed* an assignment, so the assignee comes from `mr.assigned_worker`/`assigned_manager` — a request reassigned twice shows its current assignee on both entries. Matching the web was chosen over diverging; fixing it properly needs the backend to record the assignee in log metadata
- **Bug fix — activity logs and comments came back with no person attached.** Neither `getActivityLogs` nor `getComments` sent a `populate`, and GORM only serializes explicitly requested relations, so `performed_by_client_user` / `created_by_client_user` were always null — history entries could not be attributed and every comment rendered as "Unknown". Now send `PerformedByClientUser,PerformedByClientUser.User,PerformedByTenant` and `CreatedByClientUser,CreatedByClientUser.User` respectively, matching the web portal exactly. Third instance of this class of bug in the repo, after the `getMyProperties` `populate=Property` fix logged on 2026-07-25 — it fails silently rather than erroring, which is why it keeps recurring
- New `test/api/maintenance_request_api_test.dart` pins the populate list of all four detail reads. It overrides `AbstractApi.execute` (the single choke point every call goes through) to capture the request path, so no HTTP mocking or production seam is needed. Expenses deliberately assert *no* populate: the web populates `Invoices` there, but the mobile tab shows only amount/description/date
- `MaintenanceRequestModel` gained `created_by_tenant_id`, `created_by_client_user_id` and a minimal `MaintenanceTenantModel` (`created_by_tenant`) — the fields the CREATED branch needs to tell "created by a manager" from "submitted by a tenant"
- **Bug fix — `RLBtn` labels overflowed instead of ellipsizing.** The label `Text` sat unconstrained in a `mainAxisSize.max` Row, so any full-width button whose label outgrew its box threw a RenderFlex overflow rather than truncating. Surfaced by the detail screen's two-button action bar at 400pt (it would break on a 375pt iPhone SE); wrapped in `Flexible` with `maxLines: 1` + ellipsis. Shared primitive, so this fixes every long-labelled button in the app
- History-tab widget tests cover all four sentence branches. They also pin the test surface to 400×3000: the screen is a lazy `ListView`, and at the default 600pt height the timeline is never built, so assertions about it silently pass by matching identical text in the Assignments card above
- Design doc: `docs/superpowers/specs/2026-07-26-mr-detail-mobile-redesign-design.md`

## 2026-07-25 — PermissionGuard widget; maintenance filter fixes
- Ported the web app's `PermissionGuard`/`useHasPermissions` pattern (`app/components/permissions/`) to mobile: new `shared/permission_guard.dart` with `hasPermission(ref, {roles})` and a `PermissionGuard` widget (renders `SizedBox.shrink()` instead of `null`, since Flutter widgets can't return `null`). Client-level role check only (`ClientUserModel.role`); the web app's property-scoped `PropertyPermissionGuard` variant is out of scope until pm_mobile has a per-property role context. UI-hiding convenience only, not a security boundary — backend already enforces this. Applied to the Properties screen's FAB and header `+` button, gated to `['OWNER', 'ADMIN']`. Design doc: `docs/superpowers/specs/2026-07-25-pm-mobile-permission-guard-design.md`
- Fixed a Riverpod "modify provider while widget tree was building" crash on maintenance-request filtering: `_StatusColumnPageState.didUpdateWidget` (`activity/maintenance_board.dart`) called `loadFirstPage` synchronously during the widget-update lifecycle; now deferred via `addPostFrameCallback`, matching the existing safe pattern already used in `initState`
- Capped the maintenance filter bottom sheet (`_FilterSheet`) at 80% of screen height so long option lists (e.g. many properties/units) scroll internally instead of pushing the sheet off-screen
- Fixed the maintenance board's Property filter (and the Properties tab) returning empty: `ClientUserPropertyApi.getMyProperties` wasn't sending `populate=Property`, so the backend returned every row with `"property": null` (GORM only preloads/serializes relations that are explicitly requested via `populate`) — matched the web app's request shape, which already sends this param
- `getMyProperties` extended to accept the same pagination/search/`property_status` params as `PropertyApi.getProperties`, returning a `PropertiesPage` (rows + meta) instead of a flat list; both `PropertiesNotifier` (Properties tab) and `maintenanceFilterProperties` now go through this one endpoint unconditionally (no role branching — access is always explicit via `ClientUserProperty` rows, matching the web app's approach exactly). `PropertyService.CreateProperty` (backend) already auto-links the client's OWNER to every property it creates, so this should be complete for all normally-created properties; older/seeded properties created outside that flow may still need a data backfill if they're missing the link
- Modules affected: `api/client_user_property_api.dart`, `repository/notifiers/properties/properties_notifier.dart`, `repository/providers/activity/maintenance_filter_options_provider.dart`, `modules/main/activity/maintenance_board.dart`, `modules/main/properties/root.dart`, `shared/permission_guard.dart` (new)

## 2026-07-25 — Maintenance board Property & Unit filters (multi-select)
- Added Property and Unit filter chips to the maintenance board — both multi-select (unlike the existing 4, which are single-select), sending repeated `property_id`/`unit_id` query params
- `_FilterSheet` gained a multi-select mode (checkboxes, no auto-close on tap, "Apply" button) alongside its existing single-select mode; `_FilterPickResult` gained `values`/`ids` fields
- Property filter reuses the existing `GET .../properties/me` endpoint (no backend change); Unit filter required a **new backend endpoint**, `GET .../clients/{client_id}/units`, added to the existing access-scoped cross-property "mobile" route group (same middleware as `/leases`, `/tenants`, `/maintenance-requests`)
- **Security fix found and applied while building the new units endpoint**: the units repository had no client-scoping for an unrestricted (OWNER) query with no property filter — would have leaked units across every client. Added `unitClientIDScope` (mirrors `mrClientIDScope`'s subquery pattern) and threaded it through
- Backend: `unit_id` on the maintenance-requests list endpoints is now multi-value (`UnitIDs []string`/`mrUnitIDsScope`, mirroring the existing `PropertyIDs` pattern), fixing 3 call sites including a previously-unnoticed `CountByStatus`
- New `repository/providers/activity/maintenance_filter_options_provider.dart` — 2 simple query providers, not full notifiers, for the two new option lists
- Design doc: `docs/superpowers/specs/2026-07-25-pm-mobile-maintenance-property-unit-filters-design.md`
- Modules affected: `api/`, `repository/notifiers/activity/`, `repository/providers/activity/` (new), `modules/main/activity/`, `services/main/internal/{handlers,repository,router}/`

## 2026-07-24 — Maintenance board real API integration
- Backend fix (`services/main/internal/transformations/maintenance-request.go`): `DBMaintenanceRequestToRest` now populates `unit` via `DBAdminUnitToRest` (was the leaner `DBUnitToRest`, which dropped `property_id`) — matches the response struct's own declared `AdminOutputUnit` type and every other field's admin-richness. Unblocks reading a request's property both for the assignee-filter lookup and for building the status-update mutation's per-property PATCH path.
- New: `api/maintenance_request_api.dart` (`MaintenanceRequestApi.getMaintenanceRequests()` against the cross-property "mobile" list route; `.updateStatus()` against the per-property status route), `api/client_user_property_api.dart` (`ClientUserPropertyApi.getClientUserProperties()`)
- New: `repository/models/maintenance_request_model.dart` (`MaintenanceRequestModel`, `MaintenanceUnitModel`, `MaintenanceAssigneeModel`), `lib/maintenance_utils.dart` (bidirectional status/priority/category label↔API-enum mapping)
- New: `repository/notifiers/activity/maintenance_requests_notifier.dart` — `MaintenanceRequestsNotifier`, this app's first Riverpod **family** notifier (keyed by status label), following the existing `PropertiesNotifier`/`UnitsNotifier` pagination convention; the board holds 5 simultaneous instances, one per column, mirroring the web Kanban board's 5 independent per-status queries rather than a single client-side-bucketed fetch
- New: `repository/notifiers/activity/maintenance_request_status_notifier.dart` (drag-driven status-change mutation) and `repository/notifiers/activity/maintenance_assignees_notifier.dart` (derives the Assigned Worker/Manager filter option list from client-user-properties, per distinct property currently represented on the board, cached per property, not bucketed by role)
- All 4 filter chips (Priority, Category, Assigned Worker, Assigned Manager) now send real query params, applied across all 5 columns simultaneously; dropped the mock's "Unassigned" filter option since the backend has no `IS NULL` scope for `assigned_worker_id`/`assigned_manager_id`
- Drag-to-move now calls the real status-update endpoint; on success both the source and destination columns reload with the board's current filters, on failure the move doesn't happen and a `SnackBar` reports the error (matching this app's existing error-surfacing convention) — the move now completes on mutation-success rather than instantly-then-reconciled, a deliberate simplification versus a true optimistic splice across two independent family notifier instances
- Rewrote `activity/maintenance_board.dart`'s data layer end-to-end (mock `_kMaint`/`_MaintData` removed); its UI/interaction shell (swipeable pages, page dots, filter sheets, cancel-reason sheet, edge-drag paging) from the two prior specs is unchanged
- Modules affected: `api/`, `lib/`, `repository/models/`, `repository/notifiers/activity/` (new), `modules/main/activity/`, `test/modules/main/activity/`, `test/lib/` (new), `services/main/internal/transformations/`

## 2026-07-24 — Maintenance board redesign: Jira-style paging + filters
- Redesigned the maintenance Kanban board's navigation chrome from a pinned status-chip strip + `TabBarView` to full-width swipeable status columns (`PageView`, `viewportFraction: 0.92` for a next-column peek) with a per-column header bar and a tappable page-dot indicator row, matching a Jira mobile board reference
- Drag-and-drop now auto-pages the board when a dragged card nears either screen edge, since visible chip drop-targets no longer exist; the whole current page is the drop target
- Added 4 single-select filter chips (Priority, Category, Assigned Worker, Assigned Manager) above the board, each opening a bottom-sheet picker (radio rows, Clear, X close), mirroring the web Kanban board's filter set; filters apply across all 5 columns and their live counts
- `_MaintData` gained `code` and split `assignedWorker`/`assignedManager` fields (previously one `assigned` field); cards now show `#code` and a dual worker/manager avatar row
- Design doc: `docs/superpowers/specs/2026-07-24-pm-mobile-maintenance-kanban-redesign-design.md` (supersedes the original board design)
- Modules affected: `modules/main/activity/`

## 2026-07-24 — Kanban-style maintenance board on PM mobile Activity tab
- Replaced the Activity tab's grouped maintenance list with a swipeable Kanban-style board (`activity/maintenance_board.dart`): pinned status-chip strip (New/In Progress/In Review/Resolved/Cancelled) + `TabBarView` pages, cards draggable via `LongPressDraggable`/`DragTarget` to change status
- Resolved and Cancelled moves require confirmation (Cancelled additionally requires a reason via a bottom sheet)
- Existing `_MaintCard` visuals preserved unchanged; still mock data only, no backend wiring
- Modules affected: `modules/main/activity/`

## 2026-07-24 — Create property integration
- Wired the existing mocked 4-step "Add property" wizard (`properties/add.dart`) to real data — the last mocked piece of the properties module's create/read/update/delete surface (update itself stays out of scope).
- New `PropertyApi.createProperty()` — `POST .../properties`, sends `gps_address` (see backend fix below).
- New `CreatePropertyNotifier` (`repository/notifiers/properties/create_property_notifier.dart`) — orchestrates create property → always create a default "Main" block → (SINGLE only) a default draft unit, mirroring the web wizard's server-action sequence exactly (reuses the already-real `PropertyBlockApi.createBlock()`/`UnitApi.createUnit()`, no changes needed there).
- New `PlacesApi` (`api/places_api.dart`) — Google Places Autocomplete/Details called directly over REST, no native Maps SDK or iOS/Android platform config. New `AddressSearchField` widget (`properties/address_search_field.dart`) replaces the wizard's plain address text field + fake "Map preview" box with a debounced typeahead + resolved-location confirmation card.
- New `lib/property_create_mappers.dart` — pure functions mapping the wizard's UI labels (type/status/rental mode) to backend API values, used only at submit time; the step UI itself is unchanged.
- New `.env`/`.env.example` + `flutter_dotenv` dependency — holds `GOOGLE_PLACES_API_KEY`, loaded once in `main.dart`, gitignored.
- Step 2's photo picker is now the real `UImagePicker` (reused from the unit/block forms). Submit shows a loading state, a danger-toned inline error banner on failure, and on success resets to the Properties tab before pushing the new property's detail page (so back-navigation has somewhere to go — same pattern as the delete flow's post-delete navigation).
- **Backend fix**: `CreatePropertyRequest`/`UpdatePropertyRequest` (`services/main/internal/handlers/property.go`) tagged GPS address `json:"gpsAddress"` while every other field is snake_case and the response DTO already used `gps_address` — meaning the web client's existing `gps_address` payload was silently failing to persist. Both structs now use `gps_address`; Swagger regenerated. This also fixes the same bug for the web app for free.
- Modules affected: `lib/property_create_mappers.dart` (new), `api/places_api.dart` (new), `api/property_api.dart` (extended), `properties/address_search_field.dart` (new), `repository/notifiers/properties/create_property_notifier.dart` (new), `properties/add.dart` (rewired), `.env`/`.env.example`/`.gitignore`/`pubspec.yaml`/`main.dart` (new dotenv infra), `services/main/internal/handlers/property.go` (backend fix).

## 2026-07-24 — Lease detail screen redesign
- Redesigned `LeaseDetailScreen` (`leases/detail.dart`) to match the imported "Lease Detail" mock (Claude Design `rentloop` project → `rl-mgr-lease.jsx`'s `ScreenLease`), reusing existing data/providers — no backend changes.
- New `LeaseHeroCard` (`leases/hero_card.dart`, extracted from the former `_LeaseSidebarCard`): unit photo (or tinted placeholder) with a floating status pill, tappable tenant/unit rows, rent fee, and — Active leases only — a term-progress block (new `leaseTermProgress()` in `lib/lease_status.dart`: % elapsed, "Month X of Y", "N days left" turning warning-orange at ≤14 days). Pending leases keep the inline "Start Lease" button in the same card.
- New `TermsGrid`/`TermCell` (`leases/terms_grid.dart`) — a two-column key/value grid, replacing the single-column `_InfoCard`/`_FieldRow` layout for the Lease Terms and Financial Terms cards (Tenant tab's profile cards keep the old single-column layout).
- Tab control swapped from `RLFilterChips` to `RLSegmented` (already built, previously unused by this screen); tab labels shortened (`Lease`/`Tenant`/`Docs`/`Expenses`).
- Tenant tab gained working Call/Message buttons (`tel:`/`sms:` via `url_launcher`, phone whitespace stripped before building the URI).
- New `_LeaseStickyActionBar`: for an Active lease, a persistent bottom bar with dimmed, non-interactive Renew/Terminate Lease buttons (moved out of the hero card) — both stay disabled since no backend endpoint exists for either action yet.
- New test: `test/lib/lease_status_test.dart` (4 cases for `leaseTermProgress`).
- Modules affected: `lib/lease_status.dart` (extended), `modules/main/leases/detail.dart` (rewritten layout, dead `_LeaseSidebarCard`/old `_LinkRow`/`_DateRow` deleted), `modules/main/leases/hero_card.dart` (new), `modules/main/leases/terms_grid.dart` (new).

## 2026-07-24 — Delete property flow + Archived properties/restore (real)
- Redesigned the property settings Danger Zone card (`properties/settings/hub.dart`) from a non-functional "Archive property" mock `AlertDialog` into a real "Delete property" card, ported from the Figma "Delete Property Flow" spec (same design source already implemented on the web portal).
- New `PropertyDeleteScreen` (`properties/settings/delete.dart`, `/properties/:id/settings/delete`) — one screen, internal stage state: fetches `GET .../deletion:preview`, branches on `can_delete` into a Blocked view (blocking leases/bookings/applications, "Resolve" deep-links to `/more/leases` for lease blockers) or a Confirm view (impact summary from `will_be_deleted`, type-the-property-name-to-confirm gated client-side only, matching the web portal's own validation), calls `DELETE .../{property_id}` on confirm, ends on a local Done stage.
- New, net-new on mobile: Archived Properties list + Restore flow (`modules/main/more/archived_properties.dart`, `/more/archived-properties`), reached from a new "Archived properties" row in More → Organisation settings. Paginated list via `GET .../properties?archived=true&populate=DeletedBy.User`; tapping Restore opens a bottom sheet backed by `GET .../restore:preview` with a confirm button wired to `POST .../{property_id}:restore`.
- New models: `PropertyDeletionBlockingReasonModel`/`PropertyDeletionSummaryModel`/`PropertyDeletionPreviewModel`/`PropertyRestorePreviewModel` (`repository/models/property_deletion_model.dart`); `PropertyModel` gained nullable `deletedAt`/`deletedBy` (only populated on archived rows).
- New notifiers: `DeletePropertyNotifier`, `RestorePropertyNotifier` (mirror `DeleteUnitNotifier`'s `ApiState` pattern); `ArchivedPropertiesNotifier` (mirrors `PropertiesNotifier`'s paginated-list pattern, fixed to `archived: true`). New query providers: `propertyDeletionPreviewProvider`, `propertyRestorePreviewProvider`.
- `PropertyApi.getProperties()` gained an `archived` bool param — confirmed via the Go handler that this is a separate query param from `status`, not reusable through it.
- `DeletePropertyNotifier` special-cases the backend's `PropertyHasActiveOccupancy` error (a stale eligibility check — someone else added a lease/booking/application between the preview fetch and the delete attempt): the screen refetches the deletion preview and falls through to the Blocked view instead of showing a generic error, mirroring the web portal's own re-check.
- Modules affected: `api/property_api.dart`, `repository/models/` (`property_model.dart`, new `property_deletion_model.dart`), `repository/notifiers/properties/` (new `delete_property_notifier.dart`, `restore_property_notifier.dart`, `archived_properties_notifier.dart`), `repository/providers/properties/` (new `property_deletion_preview_provider.dart`, `property_restore_preview_provider.dart`), `modules/main/properties/settings/` (`hub.dart`, new `delete.dart`), `modules/main/more/` (new `archived_properties.dart`, `root.dart`), `navigation/routes.dart`, `lib/api_error_messages.dart`.

## 2026-07-17 — Lease document management + e-signing (real, with explicit web hand-offs)
- Documents tab rewritten from static agreement-URL links into a real document pipeline view, mirroring the web `LeaseAgreementDocumentSetup`/`SigningSection` state machine (no document → MANUAL/ONLINE → DRAFT → FINALIZED/SIGNING/SIGNED), by explicit user request with two named exceptions: **starting a new document** (upload a file or pick from the template library) and **editing a document** (the Lexical rich-text editor) both show an in-app alert explaining mobile doesn't support that yet, with a "Continue on Web" button deep-linking to the right web page (`leaseOccupancyUrl()`/`leaseDocumentEditorUrl()`, new in `constants.dart`). Requesting signatures and the property manager's own signing are real, per the same request.
- **Requesting a signature** (Tenant only — witnesses are a documented gap below): confirm dialog → `POST .../signing-tokens` → shows Pending/Resend + a "Copy Link to Sign" action (copies the public `/sign/{token}` URL to the clipboard, e.g. to send over WhatsApp).
- **The property manager's own signing** is a new standalone native signature pad (`SignatureCaptureScreen`) — not a port of the web's in-document signing UX (which draws signature fields inline inside a full Lexical render, a rendering-engine-scale sub-system genuinely out of scope), but functionally equivalent: draw with a finger, capture via `RenderRepaintBoundary` → PNG bytes (no new drawing package, no temp file — `R2UploadService` gained an `uploadBytes()` using `MultipartFile.fromBytes`), upload, then `POST .../signing`. A "Review the full document on web" link sits above the pad since mobile can't render the content being signed.
- **Discovered and worked around two swapped swagger docstrings** in `services/main/internal/handlers/signing.go`: `GenerateToken`'s godoc says `.../signing` but the real chi route (confirmed in `internal/router/client-user.go:246-253`) is `.../signing-tokens`; `SignDocumentPM`'s godoc says `.../signing/direct` but the real route is `.../signing`. `SigningApi` uses the real routes, documented inline so this doesn't get silently "fixed" back to the wrong paths later.
- MANUAL-mode documents (uploaded PDF, treated as already signed) get a "Done" action wired to a real `LeaseApi.updateLease(leaseAgreementDocumentUrl: ...)` call — the one piece of the upload/create flow that doesn't depend on anything mobile can't build, so it wasn't excluded.
- **Known, documented gaps** (not silently dropped — each has a comment at its call site): witness signers (`PM_WITNESS`/`TENANT_WITNESS`) aren't shown — the web derives them by parsing signature nodes out of the document's Lexical JSON content, which mobile never renders. Generating the final signed PDF once every signature is in ("Done — Generate PDF" on web) also depends on the same Lexical renderer (`lexicalToPdf`) — mobile shows an info banner pointing to the web signing page instead of attempting it. Both create/edit/delete/request-signature/sign actions are server-enforced to the property-scoped `MANAGER` role, same access-control gap already noted for checklists — mobile has no fetch for that role yet, so the backend's 403 surfaces through the normal error path rather than being hidden by the UI.
- New: `api/lease_agreement_document_api.dart`, `api/signing_api.dart`, `repository/models/lease_agreement_document_model.dart` (`LeaseAgreementDocumentModel`, `DocumentSignatureModel`), `repository/models/signing_token_model.dart`, `repository/providers/leases/{lease_agreement_document,signing_tokens}_provider.dart`, `repository/notifiers/leases/{delete_lease_agreement_document,link_document,generate_signing_token,resend_signing_token,sign_as_manager}_notifier.dart`, `modules/main/leases/documents_tab.dart`, `modules/main/leases/signature_capture_screen.dart`. Extended: `api/lease_api.dart` (`updateLease` gained `leaseAgreementDocumentUrl`), `api/r2_upload_service.dart` (`uploadBytes()`), `constants.dart` (`leaseOccupancyUrl()`, `leaseDocumentEditorUrl()`, `leaseDocumentSigningUrl()`), `modules/main/leases/detail.dart` (Documents tab swapped in).

## 2026-07-17 — Lease checklist / inspection reports (real CRUD)
- Full checklist workflow, ported from `apps/property-manager`'s `checklist-modal.tsx`/`create-checklist-dialog.tsx`/`checklist-item-dialog.tsx`: create a report (Move-In/Move-Out/Routine, confirm dialog with type-specific copy — CHECK_OUT gets an extra deposit-risk warning, ROUTINE gets mobile-only copy since web's dialog actually reuses the CHECK_OUT copy for it, a web quirk not worth porting), add/edit/delete items (description, condition status, notes — photos excluded, dead capability with no upload UI anywhere in the product), submit for tenant review (blocked while any item is still PENDING, matching the backend's own `ChecklistItemsNotFilled` rule), read-only acknowledgment history.
- New screen `leases/checklist_detail.dart` (`ChecklistDetailScreen`, pushed via `Navigator`, not a GoRouter route — always reached from the lease detail screen with full context already in hand), `leases/checklist_item_sheet.dart` (add/edit item bottom sheet), `leases/create_checklist_dialog.dart` (create confirm dialog). Items are grouped by category client-side, splitting `description` on `" - "` — the same convention as web, not a real backend field.
- `LeaseChecklistApi` extended with `createChecklist`/`submitChecklist`/`createItem`/`updateItem`/`deleteItem`; `LeaseChecklistModel`/`LeaseChecklistItemModel` extended with the fields the read-only pass had trimmed (id/description/notes/photos on items; round/checkInChecklistId/timestamps on the checklist), plus a new `LeaseChecklistAcknowledgmentModel`. Five new `ApiState` mutation notifiers under `repository/notifiers/leases/`.
- **Known gap, deliberately not built**: submit/create/update/delete are server-enforced to the property-scoped `MANAGER` role (distinct from the workspace-level `OWNER`/`ADMIN`/`STAFF` role this app already tracks) — mobile has no fetch for that role anywhere yet, so a non-manager's mutation attempts will 403 via the existing `ApiException`/`translateApiErrorMessage` path rather than being hidden client-side up front. Building that permission check out is a separate piece of work.
- All "Create/Complete Report" buttons in the lease detail alerts, the Inspection Reports card's row taps, and its "+ {type}" ghost buttons now navigate for real instead of pushing `RLComingSoon`.
- Modules affected: `api/lease_checklist_api.dart`, `repository/models/lease_checklist_model.dart`, `lib/lease_status.dart` (all extended), `repository/notifiers/leases/{create,submit,create_checklist_item,update_checklist_item,delete_checklist_item}_notifier.dart` (new), `modules/main/leases/{checklist_detail,checklist_item_sheet,create_checklist_dialog}.dart` (new), `modules/main/leases/detail.dart` (wired).

## 2026-07-17 — Single lease detail page (real)
- `LeaseDetailScreen` (`/more/leases/:id`) rewritten from its `RLComingSoon` placeholder into a real page, ported from the web lease detail page (`apps/property-manager/app/modules/properties/property/occupancy/leases/lease/index.tsx`), stacked single-column instead of sidebar+tabs to fit mobile width.
- New `LeaseApi.getLease()`/`updateLease()`/`activateLease()`, `LeaseChecklistApi` (read-only list), `leaseDetailProvider`/`leaseChecklistsProvider` family providers, `StartLeaseNotifier`, `lease_status.dart` (ports `lease-checklist.utils.ts`'s alert-visibility rules verbatim, including a pre-existing web quirk where `stay_duration_frequency`'s plural values don't match the period-lookup table's singular keys).
- **Sidebar**: code+status, tappable tenant/unit links, rent fee, created/updated dates, "View Application" link, a footer button — Terminate Lease (Active leases, rendered disabled to match the web page's own `disabled` button — termination has no working implementation anywhere yet) or Start Lease (Pending leases, real).
- **Start Lease is the one fully real mutating action**: a new bottom sheet (`start_lease_sheet.dart`) ports the web `StartLeaseDialog` — confirms `utility_transfers_date`, then either saves it or saves-and-activates (`PATCH .../leases/{id}` then `PATCH .../leases/{id}/status:active`).
- **Alert banners** ("Lease Starting Soon"/"Lease Ending Soon") ported with their exact conditional logic, each showing a Move-In/Move-Out Report row and one lease-status row. Report rows' "Create/Complete Report" buttons open `RLComingSoon` — checklist creation/editing (item-level pass/fail, photos, submit/dispute workflow) is a deep sub-system deliberately deferred; the tenant-disputed-checklist alert variant wasn't ported for the same reason.
- **Tabs**: Lease Details (Lease Terms + Financial Terms from `lease.tenantApplication` + a read-only Inspection Reports list), Tenant Profile (full profile — extended `TenantModel` with 16 new fields, matching `DBAdminTenantToRest`'s actual map literal; two fields the web page references, `current_address`/`employer_type`, don't exist on the backend `Tenant` model at all and were excluded), Documents (agreement/termination links only, no e-sign setup wizard). Expenses stays `_ComingSoonTab` — its own ledger sub-system.
- Discovered while reading `DBAdminLeaseToRest`'s map literal: the termination-agreement signed-at fields are declared on the Go struct (and referenced by the web frontend) but never actually assigned into the response map, so they're always `null` — excluded from `LeaseModel` rather than modeling data that can never arrive.
- Modules affected: `api/lease_api.dart`, `api/lease_checklist_api.dart` (new), `repository/models/lease_model.dart`, `tenant_model.dart` (extended), `tenant_application_model.dart`, `lease_checklist_model.dart` (new), `repository/providers/leases/` (new), `repository/notifiers/leases/start_lease_notifier.dart` (new), `lib/lease_status.dart` (new), `modules/main/leases/detail.dart` (rewritten), `start_lease_sheet.dart` (new), `navigation/routes.dart`, `modules/main/leases/root.dart`.

## 2026-07-17 — Global leases list (real) + property filter
- `LeasesScreen` (`/more/leases`) is new — real from the start, mirrors `TenantsScreen`'s design exactly. New `LeaseApi`/`LeaseModel`/`LeasesNotifier` (paginated, search by code + 5-way status filter, mirrors `TenantsNotifier`).
- Backend endpoint already existed, no new work needed: `GET /api/v1/admin/clients/{client_id}/leases` (`ListLeasesAcrossProperties`, shipped in the same #374 PR as the tenants endpoint, likewise unconsumed by any frontend until now). `status` here is the full dotted `Lease.Status.*` enum (`Pending`/`Active`/`Completed`/`Cancelled`/`Terminated`) — confirmed against the web's property-scoped leases table (`apps/property-manager/.../occupancy/leases/`) to keep the same 5 values and labels. Search maps to `query`+`search_fields=code`.
- `unit`/`tenant` on each lease reuse `UnitModel`/`TenantModel` directly (same backend transforms as elsewhere) — request always sends `populate=Unit,Tenant` so they're present.
- **One leases page, multiple entry points**, same pattern as tenants: the property detail page's "Leases" Manage-grid tile now pushes `/more/leases?property_id=...&property_name=...` with a removable "Property: {name}" chip.
- Lease detail (`/more/leases/:id`) is a coming-soon placeholder (`RLComingSoon`, mirrors `more/documents.dart`) — no lease detail page exists yet to build a real one against.
- Modules affected: `api/lease_api.dart` (new), `repository/models/lease_model.dart` (new), `repository/notifiers/leases/leases_notifier.dart` (new), `modules/main/leases/root.dart` (new), `modules/main/leases/detail.dart` (new), `modules/main/properties/detail.dart`, `modules/main/more/root.dart`, `navigation/routes.dart`.

## 2026-07-17 — Fix `recent_lease` always null on cross-property tenant queries (backend)
- User noticed every tenant row on the new global tenants list showed "No lease" even for tenants that should have one. Traced to `services/main/internal/repository/tenant.go`: the `Leases`/`Bookings` preload that populates `recent_lease` was gated behind `len(*filterQuery.PropertyIDs) == 1` — so it silently never ran for an unfiltered cross-property call (`PropertyIDs` is `nil` there) or a multi-property-scoped one.
- Fixed by changing `recentLeasePreloadScope`/`recentBookingPreloadScope` to accept the same `(propertyIDs, clientID)` pair as the existing `propertyTenantsWithStatusScope`, reusing its `propertyMatchCondition` helper for an `IN (...)`/client-wide condition instead of a single `= ?` match — the preload now always runs, correctly scoped regardless of how many properties are in play. `services/main`, not `pm_mobile`; not committed (outside this repo's/branch's normal scope, left for separate review).

## 2026-07-17 — Tenant lease-status pill fixes
- `statusTone()` (`shared/tokens.dart`) now maps `'Terminated'` to the danger/red tone (was falling through to neutral) — used by both the tenants and leases lists.
- The tenants list's no-lease fallback label changed from `'No lease'` to `'Inactive'`, and its pill now always runs through `statusTone()` instead of a hardcoded neutral tone (works out the same, since `'Inactive'` isn't explicitly mapped either — but keeps the logic in one place).

## 2026-07-17 — Global tenants list (real) + property filter
- `TenantsScreen` (`/more/tenants`) is now real — replaced the fully mocked static-seed-data screen. New `TenantApi`/`TenantModel`/`TenantsNotifier` (paginated, search + status filter, mirrors `BlocksNotifier`/`UnitsNotifier`).
- Discovered the backend endpoint already existed and needed no new work: `GET /api/v1/admin/clients/{client_id}/tenants` (`ListTenantsAcrossProperties`, shipped in #374 but never consumed by any frontend — confirmed via `git show --stat` that the commit was backend-only). Supports a repeatable `property_id` filter, `status` (`ACTIVE`/`EXPIRED` — has vs. doesn't have an active lease), search (`query`+`search_fields=first_name,last_name,phone`), and standard pagination. Access-scoped server-side (`ResolvePropertyScopeFilter` — OWNER sees everything, ADMIN/STAFF only assigned properties; a disallowed `property_id` 403s the whole request rather than being silently dropped).
- Response nests a `recent_lease` (status, rent, dates, and a `unit` object) per tenant — the `unit` shape is byte-for-byte identical to the existing `UnitModel` (backend uses the same `DBAdminUnitToRest` transform for both), so `TenantLeaseRef.unit` reuses `UnitModel` directly instead of a parallel type.
- **One tenants page, multiple entry points** (not a separate property-scoped screen): the property detail page's "Tenants" Manage-grid tile now pushes `/more/tenants?property_id=...&property_name=...`; `TenantsScreen` shows a removable "Property: {name}" filter chip when arriving this way — exact same `?block_id=...` pattern already used for units-by-block, just a different field name and no dedicated screen since this one already covers "list tenants, optionally filtered."
- **Dropped from the mock**: the "balance owed" figure the old static data showed — this endpoint has no balance field (that lives in invoices/expenses, a separate #374 endpoint not integrated here). Tenant detail (`/more/tenants/:id`) stays mocked for now.
- Modules affected: `api/tenant_api.dart` (new), `repository/models/tenant_model.dart` (new), `repository/notifiers/tenants/tenants_notifier.dart` (new), `modules/main/tenants/root.dart` (rewritten), `modules/main/properties/detail.dart`, `navigation/routes.dart`.

## 2026-07-17 — Block delete restrictions + info icon
- Block creation and deletion are now both gated to `property.type == 'MULTI'` (previously only the entry points into the create flow were gated — the `BlocksListScreen` FAB and the delete action itself weren't). Viewing/editing blocks stays available on SINGLE properties.
- Deleting a block on a MULTI property now additionally requires `unitsCount == 0` — the "⋯" sheet's Delete row is disabled with an inline reason ("This block has units — move or delete them first" / "Single-unit properties can't delete blocks") rather than opening a confirm dialog that would just fail. This goes beyond what the web UI restricts (confirmed it has no such guard) — an explicit ask, not a web-parity decision like the rest of the blocks work.
- Added an info icon to the Blocks list header (`RLBackHeader.trailing`) opening a short "What are blocks?" explainer dialog.

## 2026-07-17 — Property blocks CRUD + units-by-block filter
- Property blocks are now fully CRUD from mobile (previously read-only, used only for the add-unit block picker and the Manage grid count). Confirmed via the web app's block modules first: no block detail page exists there (flat list → edit directly, or a "⋯" menu → Edit/Delete), block creation is gated to `property.type == 'MULTI'` (backend 404s otherwise, same as units), and deleting a block is **not** restricted by existing units (unlike deleting an occupied unit) — mirrored all three decisions as-is rather than inventing new mobile-only behavior.
- New `BlocksListScreen` (`properties/blocks_list.dart`, `/properties/:id/blocks`) — search + status filter chips + pagination (mirrors `units_list.dart`), rendered as cards (image, name, status pill, units count, created date, description snippet) rather than plain rows since blocks carry more info than a unit row. FAB → Add. Tapping a card opens Edit directly (no detail page, matches web); a "⋯" icon opens a sheet with Edit/Delete. Reachable from the property detail page's now-tappable **Blocks** Manage-grid tile.
- New `AddBlockScreen`/`EditBlockScreen` (`properties/add_block.dart`, `properties/edit_block.dart`) — Name (required), single-image upload (`UImagePicker` gained an optional `maxImages` param for this — web restricts block photos to one, unlike units' bulk upload), Description (optional), Status (Active/Maintenance/Inactive, default Active). Real `POST`/`PATCH` via new `CreateBlockNotifier`/`UpdateBlockNotifier`; delete via new `DeleteBlockNotifier` (real `DELETE`, plain confirm dialog, no unit-count guard).
- **"View Units" per block**: `UnitsQuery` gained a `blockId` field, `UnitApi.getUnits()` now sends `block_ids` (backend already supported it, just wasn't wired), and `units_list.dart` accepts `blockId`/`blockName` — passed via GoRouter query params (`?block_id=...&block_name=...`, a new pattern in this app's routing) from each block card's "View Units" button. Shows a removable "Block: {name}" filter chip above the status filters when active.
- Data layer: `PropertyBlockApi` gained `getBlock`/`createBlock`/`updateBlock`/`deleteBlock`; `PropertyBlockModel` extended with `description`/`images`/`createdAt`/`updatedAt`; new `BlocksNotifier` (paginated list state, mirrors `UnitsNotifier`) and `blockDetailProvider` (single-block family fetch, mirrors `unitDetailProvider`) — distinct from the existing `propertyBlocksProvider` (a flat, ungated, pageSize-100 fetch still used for the add-unit block picker and the Manage grid count).
- Modules affected: `api/property_block_api.dart`, `repository/models/property_block_model.dart`, `repository/notifiers/blocks/{blocks,create_block,update_block,delete_block}_notifier.dart` (new), `repository/providers/properties/block_detail_provider.dart` (new), `modules/main/properties/{blocks_list,add_block,edit_block}.dart` (new), `modules/main/properties/{detail,unit_form_widgets,units_list}.dart`, `api/unit_api.dart`, `repository/notifiers/units/units_notifier.dart`, `navigation/routes.dart`.

## 2026-07-17 — Real image upload on Add/Edit Unit
- Replaced the mocked, non-functional image placeholder on the Add/Edit Unit forms with a real, working multi-image picker + uploader (`UImagePicker` in `unit_form_widgets.dart`) — pm_mobile's first working image upload anywhere in the app.
- Discovered this didn't need new backend work: `apps/go` (the tenant Flutter app) already uploads through the property-manager web app's R2 proxy (`https://pm.rentloopapp.com/api/r2/upload`, unauthenticated by design — trusts `objectKey`+`file` blindly, already shipped in two other apps). New `R2UploadService` (`api/r2_upload_service.dart`) ports that exact implementation: `objectKey` prefixed `property/images/` (same bucket path convention the web add/edit forms use), returns the resulting public URL.
- `UImagePicker` mirrors `apps/go`'s `ManageAttachmentsWidget`: a thumbnail grid + "Add" tile (camera or photo library, via a new `image_picker` dependency), each picked image uploads immediately with a per-tile progress spinner / retry-on-failure / remove button, and a tap-to-preview full-screen swipeable viewer that handles both freshly-picked local files and already-uploaded (or, in Edit, prefilled) network images. Reports the uploaded URL list and an "any upload still in flight" flag up to the parent form; both Add and Edit block submission (with a validation message, not a disabled-looking button) while any photo is still uploading — matches the web form's `uploadingIds.length > 0` guard.
- `UnitApi.updateUnit()`/`UpdateUnitNotifier` gained an `images` param (wasn't there before — Edit previously had no way to touch images at all).
- New native permissions: iOS `NSCameraUsageDescription`/`NSPhotoLibraryUsageDescription`, Android `CAMERA`/`READ_EXTERNAL_STORAGE`/`WRITE_EXTERNAL_STORAGE` (copied from `apps/go`, reworded for unit photos).
- Modules affected: `api/r2_upload_service.dart` (new), `api/unit_api.dart`, `repository/notifiers/units/update_unit_notifier.dart`, `modules/main/properties/unit_form_widgets.dart`, `modules/main/properties/{add_unit,edit_unit}.dart`, `constants.dart` (`kR2UploadUrl`), `pubspec.yaml` (`image_picker`), `ios/Runner/Info.plist`, `android/app/src/main/AndroidManifest.xml`.

## 2026-07-17 — Unit edit/delete
- Added a settings gear icon to `unit_detail.dart`'s header (mirrors the property detail page), pushing a new `UnitSettingsHubScreen` (`properties/unit_settings.dart`, `/properties/:id/units/:unitId/settings`) — same visual pattern as `PropertySettingsHubScreen`: identity card, an "Edit" tile, and (only when `property.type == 'MULTI'`) a "Delete unit" tile in a danger-zone section, disabled with an explanatory subtitle when the unit is Occupied/PartiallyOccupied.
- Added a real, wired `EditUnitScreen` (`properties/edit_unit.dart`, `/properties/:id/units/:unitId/edit`), ported field-for-field from the web edit-unit page: same fields as Add minus Block and Status (neither is editable there either), prefilled from the current unit. Rental Information (occupancy/rent/billing cycle) is replaced with an info banner and omitted from the PATCH body when the unit is Occupied/PartiallyOccupied — matches the web's `isRentalInfoEditable` guard and the backend's own restriction. Submits via a new `UpdateUnitNotifier` to the real `PATCH .../units/{unitId}` endpoint.
- Delete wired to the real `DELETE .../units/{unitId}` endpoint via a new `DeleteUnitNotifier`, behind a confirm `AlertDialog` (mirrors `PropertySettingsHubScreen`'s archive-confirmation style). On success, invalidates the property's stats/units-preview/blocks providers and refreshes the units list, then `context.go()`s straight to the units list (skips popping back through the now-deleted unit's detail screen). Added an `'UnitIsOccupied'` case to `translateApiErrorMessage()` for the server-side 403 this can't fully be prevented client-side against (e.g. a unit gets occupied in another session mid-flow).
- Extended `UnitModel` with `maxOccupantsAllowed` (needed to prefill the edit form's occupancy stepper — wasn't previously read anywhere).
- Refactored: `add_unit.dart`'s field widgets (type/status rows, text inputs, chips, occupants stepper, bottom action bar) moved to a new shared `unit_form_widgets.dart` (public `U`-prefixed versions) since `edit_unit.dart` needed ~80% of the same UI — avoids a second ~400-line duplicate.
- Modules affected: `api/unit_api.dart` (`updateUnit()`, `deleteUnit()`), `repository/models/unit_model.dart`, `repository/notifiers/units/{update_unit,delete_unit}_notifier.dart` (new), `modules/main/properties/unit_form_widgets.dart` (new), `modules/main/properties/{edit_unit,unit_settings}.dart` (new), `modules/main/properties/{add_unit,unit_detail}.dart`, `lib/api_error_messages.dart`, `navigation/routes.dart`.

## 2026-07-17 — Add Unit flow, property blocks (read), Manage grid update
- Added a real, wired "Add Unit" flow (`properties/add_unit.dart`, `/properties/:id/units/add`), ported field-for-field from the web create-unit page: block picker (real data), unit type (5 rows), status (Draft/Available/Maintenance), name, mocked image placeholder, description, occupancy (single/shared + tenant-count stepper), currency (GHS only) + rent fee, billing cycle (Daily always, Weekly/Monthly only when the property's `modes` includes `LEASE`) — mirrors the web form's dropped fields too (no Area, no Features, in v1). Submits to the real `POST .../blocks/{blockId}/units` endpoint via a new `CreateUnitNotifier` (`ApiState` mutation pattern); on success invalidates the property's stats/units-preview/blocks providers and refreshes the units list, then pops back.
- "Add Unit" entry points, gated to `property.type == 'MULTI'` (mirrors the backend, which 404s unit-creation on SINGLE properties): a FAB on the units list, an "Add Unit" CTA in the units list's empty state, and a small add-icon next to the property detail page's "Units" section label (`RLLabel` gained an optional `trailing` widget slot for this — additive, other callers unaffected).
- New read-only property-blocks support: `PropertyBlockModel`, `PropertyBlockApi.getBlocks()` (`GET .../properties/{id}/blocks`), `propertyBlocksProvider(propertyId)` family provider. No blocks list/create/edit UI yet — a property with zero blocks shows an inline "no blocks yet" state in the picker with a deep link out to the web PM portal's blocks page (`constants.dart` gained `propertyBlocksUrl()`). Full block management (listing/create/edit) is intentionally deferred to a future pass.
- Manage grid on the property detail page: added a **Blocks** tile (now #1, real count via `propertyBlocksProvider`), moved **Units** to #2, and removed the placeholder **Calendar** tile (still 6 tiles / 2 rows of 3).
- `money.dart` gained `cedisToPesewas()` (inverse of `pesewasToCedis()`) for the rent-fee form input.
- Modules affected: `repository/models/property_block_model.dart` (new), `api/property_block_api.dart` (new), `repository/providers/properties/property_blocks_provider.dart` (new), `repository/notifiers/units/create_unit_notifier.dart` (new), `modules/main/properties/add_unit.dart` (new), `api/unit_api.dart` (`createUnit()`), `lib/money.dart`, `constants.dart`, `shared/widgets.dart` (`RLLabel.trailing`), `modules/main/properties/{detail,units_list}.dart`, `navigation/routes.dart`.

## 2026-07-17 — Single unit detail page
- Added a real, read-only single-unit page (`properties/unit_detail.dart`) at `/properties/:id/units/:unitId`, ported from the web unit detail view using the mobile design system: hero image/placeholder (same pattern as the property detail hero), status+type pills, a Rent Fee card, and an `RLFilterChips` tab strip (Details/Leases/Maintenance Requests/Tenant Reviews). Only Details renders real data (Description, Property & Block, Specifications, Timeline); the other three tabs are inline placeholders since Leases/Maintenance/Reviews have no real mobile data source yet.
- Scoped out of this pass: status-change dropdown, Edit, Delete (web has all three) — pm_mobile has no mutation endpoints wired anywhere yet, so this stays read-only like the properties/units list did on its first pass.
- Extended `UnitModel` with `description`, `paymentFrequency`, `propertyId`/`property` (nested `UnitPropertyRef`), `propertyBlockId`/`propertyBlock` (nested `UnitBlockRef`), `updatedAt`; added `UnitApi.getUnit()` (`GET .../units/{unitId}?populate=Property,PropertyBlock`) and the `unitDetailProvider(propertyId, unitId)` family provider.
- Wired the previously-inert unit row taps (both the property-detail units preview and the full units list) to push the new route.
- Modules affected: `repository/models/unit_model.dart`, `api/unit_api.dart`, `repository/providers/properties/unit_detail_provider.dart` (new), `modules/main/properties/unit_detail.dart` (new), `modules/main/properties/{detail,units_list}.dart`, `lib/unit_status.dart` (added `paymentFrequencyLabel`), `navigation/routes.dart`.

## 2026-07-17 — Units list search + status filter
- Added debounced search and a status filter (All/Available/Occupied/Maintenance/Draft) to the property-scoped units list (`units_list.dart`), mirroring the properties list's search+filter+pagination pattern.
- `UnitApi.getUnits()` gained `status`/`search` params (same `query`+`search_fields`/`status` query-string shape as `PropertyApi.getProperties()`); `UnitsNotifier` gained a `UnitsQuery` class and now threads filters through `loadFirstPage`/`loadNextPage`, resetting pagination on filter/search change.
- Modules affected: `api/unit_api.dart`, `repository/notifiers/units/units_notifier.dart`, `modules/main/properties/units_list.dart`.

## 2026-07-16 — Property detail page integration
- Wired the single-property detail screen to real data: property fetch, a Cube-sourced stats card (occupancy donut, monthly revenue) and Manage grid (Units/Tenants/Leases/Applications/Bookings counts), and a real 5-item units preview.
- Added a swipeable hero image carousel (falls back to the existing placeholder tile when a property has no images).
- Added a new paginated "all units" screen (`units_list.dart`, `/properties/:id/units`), infinite-scroll, shown via a "See all" action that's hidden below 6 total units.
- Added a new `TenantApplications` Cube model (`services/cube/model/cubes/TenantApplications.js`) so the Applications count has a real data source — no prior cube covered tenant applications.
- Modules affected: `services/cube/model/cubes/`, `apps/pm_mobile/lib/src/api/` (`unit_api.dart`, `analytics_api.dart`, `property_api.dart`), `apps/pm_mobile/lib/src/repository/` (models, providers, notifiers), `apps/pm_mobile/lib/src/modules/main/properties/` (`detail.dart`, `units_list.dart`), `apps/pm_mobile/lib/src/lib/` (`money.dart`, `unit_status.dart`, `property_stats_logic.dart`), `apps/pm_mobile/lib/src/navigation/routes.dart`.

## 2026-07-16 — Properties list integration
- Replaced the mocked property list with real, paginated data from `GET /api/v1/admin/clients/{client_id}/properties` (10/page, infinite scroll)
- Extended `PropertyModel`/`PropertyApi.getProperties()` with the real fields/params the list needed; added `PropertiesNotifier` (first paginated-list notifier in this app)
- Added a real debounced search and a status filter (All/Active/Maintenance/Inactive) to the Properties tab
- `RLSearchBar` gained an optional real-`TextField` mode
- Modules affected: `api/`, `lib/`, `repository/notifiers/`, `modules/main/properties/`, `shared/widgets.dart`

## 2026-07-11 — Login integration + real-data wiring for workspace/profile surfaces
- Implemented real login, cold-start token validation, workspace selection, and logout against `https://api.rentloopapp.com`, replacing the fully-mocked `AppStartupNotifier` (11-task plan; design spec + plan under `docs/superpowers/`)
- New: `api/root.dart` (`AbstractApi`, `ApiException`), `api/user_api.dart` (`UserApi.login()`/`getMe()`); `repository/api_state.dart`, `repository/models/{user,client_user,client}_model.dart`, `repository/notifiers/auth/login_notifier.dart`; `architecture/{secure_storage,token_manager,workspace_id_manager,current_user,current_workspace,app_startup}/`; `lib/{storage,secure_storage,token_manager,workspace_id_manager,workspace_resolution,api_error_messages}.dart`
- Deleted the old hand-written mock `architecture/app_startup.dart`; replaced with `@riverpod` code-gen `AppStartupNotifier` at `architecture/app_startup/app_startup_notifier.dart` (provider renamed `appStartupProvider` → `appStartupNotifierProvider`)
- Modified for real-data wiring: `modules/auth/login/root.dart`, `modules/auth/workspace_select/root.dart`, `modules/main/workspace_sheet.dart`, `modules/main/more/root.dart` (profile + workspace cards), `modules/main/home/root.dart` (top header only), `navigation/{splash,routes}.dart`
- Added `json_annotation` dependency (`pubspec.yaml`)
- Added tests: `test/api/root_test.dart`, `test/lib/workspace_resolution_test.dart`, `test/repository/models/*_test.dart` (17 tests total, all passing)
- Fixed during review: `isActiveClientUser` originally used a substring `contains('active')` check that misclassified `"Inactive"` as active; changed to a last-dot-segment exact match
- Still mocked: `properties/`, `tenants/`, `activity/`, `money/`, `announcements/`, and most of `more/` (members, payment accounts, documents, agreement, billing, settings) — pending their own integration passes
- Modules affected: `api/`, `architecture/`, `lib/`, `repository/`, `modules/auth/`, `modules/main/home/`, `modules/main/more/`, `modules/main/workspace_sheet.dart`, `navigation/`

## 2026-07-10 — Initial index
- First scan of codebase
- Generated architecture.md, implementation.md, patterns.md, decisions.md
- Notable finding: app is UI-only (mock data throughout), no `api/`/`repository/` layer yet — API integration work is about to begin, starting with `modules/auth/login`
